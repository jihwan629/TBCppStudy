#include <iostream>

using namespace std;

int min(int x, int y)
{
    //if (x > y)  return y;
    //else        return x;

    return (x > y) ? y : x;
}

int main()
{
    //int x;
    //cin >> x;

    //if (x > 10)
    //{
    //    cout << x << " is greater than 10" << endl;
    //    cout << "TEST 1" << endl;
    //}
    //else
    //{
    //    cout << x << " is not greater than 10" << endl;
    //    cout << "TEST 1" << endl;
    //}

    //if (1) int x = 5;
    //else int x = 6;

    //cout << x << endl;

    //if (x > 10)
    //{
    //    cout << "x is greater than 10" << endl;
    //}
    ////else if (x < 10)
    ////{
    ////    cout << "x is less than 10" << endl;
    ////}
    ////else // if (x == 10)
    ////{
    ////    cout << "x is exactly 10" << endl;
    ////}
    //else
    //{
    //    if (x < 10)
    //    {
    //        cout << "x is less than 10" << endl;
    //    }
    //    else // if (x == 10)
    //    {
    //        cout << "x is exactly 10" << endl;
    //    }
    //}

    //if (x >= 10)
    //    if (x <= 20)
    //        cout << "x is between 10 and 20" << endl;
    //    else 
    //        cout << "x is more than 20" << endl;

    //int x, y;
    //cin >> x >> y;

    //if (x == y)
    //    cout << "same numbers" << endl;
    //else
    //    cout << "Not same" << endl;

    //if (x > 0 && y > 0)
    //    cout << "both numbers are positive" << endl;
    //else if (x > 0 || y > 0)
    //    cout << "one of the numbers is positive" << endl;
    //else
    //    cout << "Neighter numbers is positive" << endl;

    int x;
    cin >> x;

    //if (x > 10)
    //    cout << "A" << endl;
    //else if (x == -1)
    //    return 0;
    //else if (x < 0)
    //    cout << "B" << endl;

    //cout << "Hello" << endl;

    //if (x > 10)
    //    ;

    if (x = 0) // x = 0; if(x)
        cout << x << endl;

    cout << x << endl; // 0

    return 0;
}

